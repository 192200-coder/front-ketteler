// Entorno de DESARROLLO (por defecto: ng serve, pruebas locales, app Capacitor en el celular).
// La IP LAN permite que el celular alcance el backend por WiFi.
export const environment = {
  production: false,
  apiBaseUrl: 'http://192.168.101.3:8001/casaketteler',
};
